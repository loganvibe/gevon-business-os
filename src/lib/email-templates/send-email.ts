/**
 * Placeholder for the Lovable Emails scaffold. When the user configures a
 * verified email domain, the scaffold replaces this file with a real
 * `sendTemplateEmail` implementation. Until then, exporting `null` lets the
 * bundler resolve the import while `email.send` job handler falls back to
 * the "queued" state.
 */
export const sendTemplateEmail: null = null;
